# GaN HEMT 紧凑模型建模 Agent 竞赛注意事项

## 总体判断

这个竞赛最容易出问题的地方，不是“Agent 能不能调用工具”，而是能否在 24 小时内稳定地产出两份物理合理、可仿真、可审计的模型卡。

## 1. 先确认官方契约，不能凭经验猜

必须尽早拿到：

- 赛题 Q&A。
- MCP 完整工具清单。
- ASM-HEMT 具体版本。
- 官方模型卡 Schema。
- QA Checklist。
- RMSE 归一化方式。
- 并发、限流、会话恢复和日志要求。

尤其要确认 `Curve_RMSE_normalized` 是否限制在 0–1、不同曲线如何加权，否则本地评分器可能与官方不一致。

## 2. 不要让 LLM 直接优化约 120 个参数

LLM 不适合直接计算具体参数值。正确分工应该是：

- LLM：分析现象、选择参数组、决定接受、回滚或 retune。
- MeQLab：执行仿真和数值拟合。
- 确定性代码：检查参数范围、QA、误差、预算和停止条件。

LLM 只能从允许动作中选择，不能自行编造参数名、物理范围或 MCP 工具。

## 3. 禁止一次性开放全部参数

ASM-HEMT 参数之间耦合很强。如果一次拟合全部参数，很容易出现：

- 可见曲线误差很低，但参数不物理。
- 参数互相补偿，隐藏网格泛化失败。
- DC 拟合改善，但 C-V、Pulse 或 S 参数恶化。
- Agent 陷入反复 retune。

必须采取“参数分组、逐步解冻、每组验收、异常回滚”的方式。

## 4. QA 应优先于局部 RMSE

单器件得分是：

`QA_pass_rate × (1 - Curve_RMSE_normalized)`

QA 是乘法项。即使 RMSE 很低，QA 通过率只有 0.5，成绩也会直接折半。

优化顺序应该是：

1. 模型卡合法且可以仿真。
2. 参数处于物理范围。
3. 消除严重单调性、对称性和 kink 问题。
4. 两套器件均达到稳定基线。
5. 最后降低 RMSE。

## 5. 较弱器件比优秀器件更重要

如果 Card 1 是较弱器件，总分实际上等价于：

`0.65 × Card_1 + 0.35 × Card_2`

所以后半程继续精修优秀器件通常不划算，应把预算转向当前较弱器件。

## 6. 两套器件必须先分别获得可提交基线

不要先把第一套器件做到极致，再开始第二套。

建议测试日前演练形成硬时间门禁：

- 第 3 小时：两套器件都完成数据审计。
- 第 6 小时：两套器件各有一份可提交模型卡。
- 第 17 小时：停止无边界探索。
- 第 20 小时：禁止引入未经回归的新策略。
- 第 22 小时：冻结模型卡并开始提交校验。

## 7. 正式数据不会下发到本地

赛题明确测试数据始终留在组委会服务器。因此不能把系统设计成“下载数据后用本地 NumPy 分析”。

需要提前确认 MCP 能返回什么：

- 原始数值还是数据摘要。
- 曲线采样点还是图片。
- 是否支持服务端筛选和统计。
- 是否能够获取拟合残差分布。
- 是否能够复用会话中的中间结果。

Agent 的判断能力必须建立在 MCP 实际可提供的信息上。

## 8. 反作弊条件必须做成程序级硬门禁

赛题明确：

- 任一测试器件 MCP 调用不足 10 次，该组得 0 分。
- 全程没有调用指定国产大模型 API，该组得 0 分。

但也不能用无意义调用凑数。每次调用都应关联：

- 当前建模节点。
- 调用目的。
- 输入摘要。
- 返回结果。
- 后续决策。

## 9. 每次接受模型卡前都要做回归式检查

不能只看当前参数组的误差。建议每轮同时比较：

- 当前目标曲线是否改善。
- 已完成曲线是否退化。
- QA 通过率是否下降。
- 参数是否接近边界。
- 隐藏验证网格代理是否改善。
- 改进是否大于最小收益阈值。

任何严重退化都应拒绝候选模型卡并回滚。

## 10. 永远不要覆盖最佳模型卡

每个器件至少保留：

- 第一份可提交基线。
- 当前最佳已验证版本。
- 当前拟合候选版本。
- 最终冻结版本。

即使后续拟合崩溃，也必须能够立即提交最近的有效模型卡。

## 11. 必须支持断点恢复和幂等重试

24 小时运行期间很可能遇到：

- MCP 超时。
- LLM 限流。
- 网络中断。
- MeQLab 会话异常。
- Agent 进程退出。
- 拟合长时间不收敛。

每次有效 MCP 调用、模型卡接受和 QA 完成后，都应保存检查点。重试之前必须判断调用是否幂等，避免重复创建会话或重复提交任务。

## 12. 不要把训练数据理解成必须训练大模型

这个赛题更需要的是：

- 合成器件数据工厂。
- 参数变化覆盖。
- 策略回归测试。
- 隐藏器件盲测。
- 故障恢复演练。

前期不建议投入大量时间微调大模型。相比微调，可靠的参数策略、物理 QA 和回滚机制更有价值。

## 13. 日志不是最后补的功能

运行日志本身就是正式交付物和反作弊证据。必须从第一天就按正式标准设计，至少覆盖：

- MCP 调用记录。
- LLM 模型与调用统计。
- Agent 决策序列。
- 参数卡版本变化。
- QA 与误差变化。
- 重试、回滚和停止原因。
- 时间与调用预算。

同时严禁记录 Token、API Key 和其他密钥。

## 14. 提交格式必须提前演练

不要等测试日最后才第一次生成正式模型卡和日志。应提前验证：

- 官方 Schema。
- SPICE/MeQLab 可加载性。
- 文件命名。
- 器件与模型卡对应关系。
- 日志完整性。
- 总结统计与日志一致性。
- 敏感信息扫描。

## 项目五条红线

1. 未取得官方 Schema 和 MCP 契约前，不写死工具名和产物字段。
2. 基础 DC 没有稳定前，不进入复杂效应提取。
3. 没有完成全局 QA，不接受局部 RMSE 更低的模型卡。
4. 没有检查点，不执行下一轮高风险拟合。
5. 正式测试最后两小时，不再修改核心算法。

## 最终结论

这个项目应当按照“工业建模自动化系统”来建设，而不是按照“LLM 对话应用”来建设。Agent 的智能程度很重要，但物理约束、数值稳定性、恢复能力和审计证据更决定最终成绩。

