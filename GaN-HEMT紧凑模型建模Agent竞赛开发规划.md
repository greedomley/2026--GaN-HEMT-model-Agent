# GaN HEMT 紧凑模型建模 Agent 竞赛开发规划

## 1. 规划依据

本规划依据《赛题三：GaN HEMT 紧凑模型建模 Agent 自动化挑战赛（概伦电子）》赛题指南整理，用于明确整个竞赛项目从准备、研发、验证到测试日交付的前后开发顺序。

对应项目框架位于：`C:\Users\ztys_010\Desktop\project\github\gan-hemt-modeling-agent`

## 2. 总体开发主线

推荐严格按照以下顺序推进：

> 规则与接口确认 → MCP 最小链路 → 人工可控建模基线 → 确定性提参引擎 → Agent 状态机 → 合成数据回归 → 容错与评分优化 → 交付冻结 → 24 小时正式运行

最关键的实施原则是：**不要一开始就开发一个只会对话的 Agent。应先证明通过 MCP 可以稳定、可重复地完成一次 ASM-HEMT 参数提取，再逐步让 Agent 接管策略选择、异常诊断和 retune 决策。**

## 3. 推荐总体技术架构

- Python 3.10 或以上版本。
- LangGraph 作为主状态机，便于图结构审阅、状态持久化和赛后审计。
- 至少接入一种指定范围内的国产大模型 API，并准备经过验证的备用模型。
- 通过 Primarius Modeling MCP Server 调用 MeQLab。
- 采用结构化 JSONL 运行日志。
- 使用持久化检查点保存 Agent 状态和模型卡版本。
- 参数边界、QA、回滚、停止条件、评分和产物校验由确定性模块负责。
- LLM 负责策略规划、下一步决策、失败诊断和总结，不直接绕过领域约束修改模型卡。

## 4. 项目开发阶段及前后顺序

### 阶段 0：赛题规则与官方契约确认

主要工作：

1. 获取并阅读赛题 Q&A。
2. 获取官方模型卡 Schema。
3. 获取 Primarius Modeling MCP Server 工具清单和参数定义。
4. 获取物理 QA Checklist。
5. 确认认证 Token、限流、超时、会话有效期和断线恢复规则。
6. 确认两套器件是否允许并行运行。
7. 确认国产大模型允许范围、调用统计和审计要求。
8. 确认提交平台、命名规范和截止规则。

阶段产物：

- 官方契约清单。
- 待确认问题清单。
- 输入、输出和评分口径说明。
- 已确认事实与暂定假设清单。

阶段验收：

- 关键接口、模型卡格式和 QA 规则不存在未标记的猜测。

### 阶段 1：基础工程环境与项目骨架

主要工作：

1. 确定 Python 和 Agent 框架版本。
2. 确定国产大模型主备方案。
3. 建立分层项目结构。
4. 设计配置、密钥、日志、检查点和产物目录。
5. 建立 `run_id`、`device_id`、会话 ID 和模型卡版本规范。
6. 明确测试数据不落地和敏感信息不入日志的边界。

阶段产物：

- 项目目录骨架。
- 总体架构说明。
- 模块职责说明。
- 环境和运行约定。

阶段验收：

- 架构边界清晰，领域逻辑不依赖具体 MCP 或 LLM SDK。

### 阶段 2：Primarius Modeling MCP 最小链路

主要工作：

1. 完成 Token 鉴权和服务健康检查。
2. 创建和关闭 MeQLab 建模会话。
3. 验证数据清点或数据引用能力。
4. 验证 ASM-HEMT 模型选择和初始化能力。
5. 验证参数读取、设置、冻结和解冻能力。
6. 验证拟合执行能力。
7. 验证 QA、可视化和模型卡导出能力。
8. 统一处理 MCP 超时、服务异常和返回结构错误。
9. 记录每次 MCP 调用的耗时、结果、错误和重试。

阶段产物：

- MCP 能力矩阵。
- 最小端到端调用记录。
- MCP 错误分类和恢复规则。

阶段验收：

- 能够从建立会话一直运行到导出一份模型卡。
- 任意失败都能被识别、记录和分类。

### 阶段 3：非 Agent 的人工可控建模基线

这一阶段暂时不让 LLM 决定流程，先以固定策略完成一次 ASM-HEMT 参数提取。

主要工作：

1. 加载一套开发器件数据。
2. 完成数据质量检查。
3. 初始化 ASM-HEMT 模型卡。
4. 建立参数分组和初始提参顺序。
5. 设置参数上下界、冻结和解冻规则。
6. 分组完成拟合。
7. 执行物理 QA。
8. 保存每轮模型卡和误差结果。
9. 形成首份可仿真、可 QA、可复现的模型卡。

阶段产物：

- 第一份建模基线模型卡。
- 参数分组表。
- 固定提参顺序。
- QA 和误差报告。
- 建模过程复现说明。

阶段验收：

- 相同输入和配置能够稳定复现结果。
- 模型卡可被指定仿真链正常加载。
- 基础物理 QA 通过。

### 阶段 4：确定性参数提取引擎

主要工作：

1. 将数据审计模块化。
2. 将参数分组、依赖和物理范围模块化。
3. 建立候选模型卡和已接受模型卡机制。
4. 建立拟合前后误差比较。
5. 建立 QA 结果比较。
6. 建立接受、有限 retune 和回滚规则。
7. 建立每个参数组的最大轮数、最小收益和停止条件。
8. 建立模型卡版本链和检查点恢复。
9. 建立时间、MCP 调用和重试预算。

阶段产物：

- 确定性建模用例。
- 参数和 QA 策略。
- 检查点与回滚机制。
- 运行状态模型。

阶段验收：

- 任意参数修改都必须经过物理范围校验。
- 新结果退化时能够自动回滚。
- 进程中断后能够从最近检查点继续。

### 阶段 5：Agent 状态机与国产大模型接入

主要工作：

1. 使用 LangGraph 建立 nodes、edges 和条件路由。
2. 建立数据审计、计划、拟合、QA、retune、回滚和导出节点。
3. 建立 Agent 状态持久化。
4. 接入国产大模型 API。
5. 约束 LLM 只能从允许动作中选择。
6. 对 LLM 输出执行结构化校验和领域校验。
7. 保存每次决策的输入事实、候选动作、选择、依据和执行结果。
8. 建立主模型失败后的国产备用模型切换。
9. 建立 LLM 限流、超时和非法输出处理。

阶段产物：

- 可审阅的 Agent 决策图。
- 完整状态定义。
- LLM 结构化决策契约。
- 决策序列日志。
- 异常重试和降级流程。

阶段验收：

- Agent 能自主完成开发器件的全流程建模。
- Agent 不存在绕过参数边界、QA、回滚或产物校验的执行路径。

### 阶段 6：合成训练数据与回归测试体系

组委会声明正式测试集为 SPICE + ASM-HEMT 合成数据，因此开发数据应围绕这一分布构造。

建议覆盖：

- 不同阈值电压。
- 不同导通能力和迁移率水平。
- 不同接触与外部寄生。
- 不同温度范围和自热强度。
- 不同陷阱、动态导通和色散特征。
- 不同 kink 和高场行为。
- 不同场板相关效应。
- 测量噪声、异常点和数据缺失。
- 参数位于物理边界附近的困难器件。

数据应划分为：

1. 开发集。
2. 回归集。
3. 严格隔离的模拟隐藏测试集。

每次系统变更至少比较：

- 全流程成功率。
- QA 通过率。
- 归一化 RMSE 代理。
- MCP 调用次数。
- LLM 调用次数和 Token。
- 总运行时间。
- 重试和回滚次数。
- 最差器件表现。

阶段验收：

- 系统不是只对一两个固定器件有效。
- 策略变更不会悄悄破坏已有物理 QA 或恢复能力。

### 阶段 7：评分导向与双器件鲁棒性优化

赛题单器件得分为：

`Card_i_Score = QA_pass_rate × (1 - Curve_RMSE_normalized)`

总分为：

`总分 = 0.7 × mean(Card_1, Card_2) + 0.3 × min(Card_1, Card_2)`

因此优化顺序必须是：

1. 模型卡合法且可以仿真。
2. 消除严重物理违规，提升 QA 通过率。
3. 保证两套器件都达到稳定基线。
4. 再降低曲线归一化 RMSE。
5. 后半程优先改善较弱器件。
6. 最后优化运行时间和调用成本。

主要工作：

- 建立自建隐藏网格评分代理。
- 建立双器件比较和预算重新分配。
- 建立最弱器件优先策略。
- 优化参数组顺序和停止策略。
- 验证不同国产大模型和 MCP 故障下的稳定性。

阶段验收：

- 多次盲测中两套器件都能稳定输出有效模型卡。
- 不依赖某一器件或单次随机运行取得好成绩。

### 阶段 8：工程容错与反作弊审计

必须覆盖的故障：

- MCP 超时、5xx 和返回格式异常。
- 国产大模型 429、超时和非法结构化输出。
- 拟合不收敛。
- 参数越过物理范围。
- QA 明显退化。
- MCP 会话丢失。
- Agent 进程重启。
- 时间或调用预算即将耗尽。

反作弊硬检查：

- 每个正式测试器件至少产生 10 次真实、有效的 MCP 调用。
- 全程至少调用一种指定范围内的国产大模型 API。
- 不通过无意义空调用凑次数。
- 日志能够证明调用与正常建模工作流有关。

阶段验收：

- 关键故障发生后能恢复或安全降级。
- 始终保留最后一份可提交模型卡。
- MCP、LLM 和决策证据链完整。

### 阶段 9：交付冻结和竞赛演练

测试日前需要准备：

- 完整代码仓库。
- Agent 图和状态定义。
- Agent 主入口。
- 依赖声明与锁定文件。
- 配置模板。
- 架构图。
- 安装和运行说明。
- 模型卡 Schema 校验器。
- 日志完整性校验器。

测试日结束前需要提交：

- 两份 ASM-HEMT 模型卡。
- 两份独立运行日志。
- 一份运行总结。

演练内容：

1. 在干净环境完成安装和启动。
2. 完整模拟 24 小时流程。
3. 演练服务中断和进程恢复。
4. 演练提前停止并生成可提交产物。
5. 校验文件命名、Schema、内容和敏感信息。
6. 冻结最终竞赛版本。

阶段验收：

- 干净机器可以按文档启动。
- 中断后可以继续。
- 能自动生成并校验全部正式产物。

## 5. ASM-HEMT 建模模块推荐顺序

具体参数名和范围必须以官方文档及 MeQLab 能力为准，但建模阶段建议按以下顺序执行。

### 5.1 数据与器件审计

- 检查 DC I-V、C-V、Pulse I-V、S 参数是否完整。
- 检查单位、端口、温度、偏置方向、器件尺寸和扫描网格。
- 标记缺失区间、异常点、噪声和不连续曲线。
- 生成数据质量报告和可执行建模模块清单。

### 5.2 模型初始化与物理边界

- 选择官方指定版本的 ASM-HEMT。
- 加载官方模板或允许的初始模型卡。
- 设置参数物理范围、依赖关系和冻结状态。
- 禁止 LLM 写入未登记或未经验证的任意参数。

### 5.3 基础 DC 参数

- 优先拟合转移特性、输出特性和低场区域。
- 识别阈值、亚阈值、导通电流、迁移率和串联电阻等基础行为。
- 基础 DC 未稳定前不进入高阶效应提取。

### 5.4 电荷与 C-V 参数

- 拟合栅电荷和各端电容行为。
- 检查电荷连续性、偏置变化趋势和物理合理性。

### 5.5 S 参数与外部寄生

- 提取接触、端口和外部寄生网络。
- 分离本征与外部寄生，防止用本征参数补偿寄生误差。

### 5.6 温度与自热

- 使用多温度数据拟合温度依赖。
- 再提取自热相关参数。
- 检查功耗增加时的热效应趋势是否合理。

### 5.7 Pulse I-V 与陷阱效应

- 提取动态导通、陷阱和色散特征。
- 避免为了降低局部误差破坏静态 DC 基线。

### 5.8 高场、kink 和场板效应

- 在基础模型稳定后处理特殊高场行为。
- 通过单调性、kink 检测和参数范围约束 retune。

### 5.9 分组联合优化

- 按参数组逐步解冻。
- 每轮保存模型卡、误差、QA 和参数变化。
- 新结果变差或发生严重物理违规时自动回滚。

### 5.10 最终 QA 与模型卡生成

- 执行完整 QA Checklist。
- 在自建隐藏网格上检查泛化能力。
- 校验官方 Schema、模型卡完整性和 SPICE 可加载性。

## 6. Agent 状态机推荐流程

```text
启动与鉴权
  → 创建器件会话
  → 数据清点与质量检查
  → 选择模型及初始化
  → 生成提参计划
  → 执行当前参数组拟合
  → 计算曲线误差
  → 运行物理 QA
  → 决定接受 / 回滚 / retune
  → 判断收敛与剩余预算
  → 全局联合优化
  → 最终 QA
  → 模型卡及日志导出
```

异常分支至少覆盖：

- MCP 超时、服务错误和响应异常。
- 国产大模型限流、超时和结构化输出失败。
- 拟合不收敛。
- 参数越界。
- QA 退化。
- 会话丢失。
- 剩余时间不足。
- 调用预算即将耗尽。

## 7. 每轮运行需要保存的状态

- `run_id`、`device_id` 和 MCP 会话 ID。
- 当前节点、当前参数组和迭代轮次。
- 参数版本、候选模型卡和回滚检查点。
- 当前和历史 RMSE 代理。
- QA 项目、严重等级和通过情况。
- MCP 调用次数、耗时、错误和重试。
- LLM 模型、调用次数、Token 和决策摘要。
- 剩余时间、迭代预算和调用预算。
- 接受、retune、回滚和停止原因。
- 最终产物位置及校验结果。

## 8. 24 小时测试日开发运行顺序

| 时间 | 工作内容 |
|---|---|
| 0–1 小时 | 鉴权、MCP 健康检查、两套器件数据清点、建立独立会话 |
| 1–3 小时 | 完成两套器件的数据 QA 和基础建模计划 |
| 3–6 小时 | 两套器件分别生成第一份可提交基线模型卡 |
| 6–13 小时 | 完成 DC、C-V、S 参数、温度、自热、Pulse 和特殊效应提取 |
| 13–17 小时 | 联合优化、物理 QA 和自动 retune |
| 17–20 小时 | 比较两套器件，集中改善较弱器件 |
| 20–22 小时 | 冻结候选卡，执行完整 Schema、QA 和复现检查 |
| 22–23 小时 | 导出两份模型卡、两份日志和一份总结 |
| 23–24 小时 | 上传、校验、人工复核并保留故障缓冲 |

如果官方 Token 或 MCP 不支持并发，应让两套器件交替推进：先分别建立可提交基线，再轮流优化，避免第一套器件耗尽大部分时间。

## 9. 项目最先应该执行的任务

第一批任务应严格按以下顺序开始：

1. 获取赛题 Q&A 和官方模型卡 Schema。
2. 获取 Primarius Modeling MCP 的工具定义和开发环境。
3. 完成 MCP 最小调用链路。
4. 暂不接入 LLM，以固定流程完成第一次完整建模。
5. 将成功路径整理成参数分组、物理 QA、回滚和停止规则。
6. 最后接入 LangGraph 和国产大模型。

完成以上六步后，项目才真正进入 Agent 自动化研发阶段。

## 10. 最终完成标准

该竞赛项目的完成标准不是“Agent 可以运行”，而是：

- 能在未知 GaN HEMT 器件上自主完成建模。
- 两套器件都能生成可仿真、物理合理的完整 ASM-HEMT 模型卡。
- 能在 24 小时内完成拟合、QA、retune 和正式产物生成。
- MCP、LLM、参数变化、QA 和决策过程全程可审计。
- 服务异常或进程中断后能够恢复。
- 最终提交物完全符合官方 Schema 和提交规范。

