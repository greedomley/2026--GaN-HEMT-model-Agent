# `contracts` 说明

用于保存经官方确认的 MCP、模型卡、运行日志、运行总结和决策记录契约。

`runtime_control/` 保存项目内部的阶段结果、操作账本、检查点、完成判定、失败记录和性能报告契约；详见 [运行控制契约说明](runtime_control/README.md)。内部契约必须与官方模型卡/日志 Schema 明确分离。

当前子目录只是占位，不包含猜测性 Schema。后续任何契约都需要标明来源、版本和确认日期。
