GaN-HEMT 建模 Agent 完整开发顺序清单
编号规则：
A：赛题约束与工程基线
B：领域层
C：端口、基础设施与治理
D：本地仿真、数据与 Mock 环境
E：确定性单器件建模
F：Agent 与 LangGraph
G：双器件、交付与测试日运行
H：官方 MCP、回归与最终发布
官方 MCP 尚未提供时，先使用内部契约、Mock MCP 和 Fake LLM 开发；官方资料到位后，只替换契约映射和 MCP 适配层。本清单暂不包含前端，前端按照后期开发执行。
A：赛题约束与工程基线

A1：整理赛题固定事实与未知事项：明确目标模型为 ASM-HEMT，建模工具为 MeQLab，外部调用通过 Primarius Modeling MCP Server，Agent 框架限定为 LangGraph 或 deepagents，大模型限定为国产大模型 API；登记两套物理特征不同的测试器件、24 小时测试窗口、DC I-V、C-V、Pulse I-V、S 参数、隐藏 (Vgs, Vds, T) 网格评分、最终交付物和反作弊要求。将“官方已确认”“项目暂定”“等待 Q&A”分开管理，任何未知规则不得被直接写入代码。

A2：固定技术路线与运行环境：根据项目架构确定 LangGraph 作为主状态机，deepagents 不与 LangGraph 重复实现；固定 Python 3.10+、目标操作系统、主国产模型、备用国产模型、网络要求、外部 SDK 版本和竞赛统一环境；明确本地开发、测试服务器和正式竞赛环境之间的差异。

A3：建立现有项目工程基线：在已有 src、contracts、config、data、artifacts、tests 和 operations 骨架上补齐依赖声明、锁定文件、源码包配置、统一入口、日志命令、测试命令、格式化、类型检查、静态检查和基础 CI 检查。

A4：定义统一标识与内部契约：统一 run_id、device_id、MCP session_id、model_card_version、event_id、trace_id、checkpoint_id、task_id 和 idempotency_key；定义内部运行记录、决策记录、模型卡版本、QA 结果、运行总结、错误对象、ActionIntent、ToolCall、ToolResult 和 Checkpoint。内部契约必须与官方模型卡 Schema 分离，避免提前猜测官方格式。
B：领域层

B1：开发基础领域对象：实现 device、dataset、run 等领域对象；描述器件身份、几何尺寸、温度、偏置、数据引用、当前阶段、运行状态、剩余时间、剩余调用预算、任务状态、deadline 和终止原因。


B2：开发 ASM-HEMT 参数分组模块：建立参数组注册表；为每个参数组定义目标曲线、输入数据、参数集合、单位、上下界、前置依赖、冻结参数、解冻参数、最大迭代次数、最小收益和停止条件；禁止使用未登记的参数名。


B3：开发模型卡版本管理模块：实现初始模型卡、候选模型卡、最佳已验证模型卡、最终冻结模型卡；记录参数来源、版本关系、参数差异、对应拟合、QA、误差、检查点、校验摘要和导出时间；失败版本不得覆盖有效版本；支持参数快照和文件类操作回退。


B4：开发拟合与 QA 领域模块：拟合模块负责目标曲线、权重、拟合状态、收敛状态、参数变化、误差结果和拟合耗时；QA 模块负责单调性、对称性、kink、参数物理范围、曲线连续性、数据覆盖和其他官方检查项的统一表示。


B5：开发评分与策略模块：实现 QA_pass_rate × (1 - Curve_RMSE_normalized) 的单器件评分代理及双器件平均分、最弱器件分数和总分代理；实现候选接受、拒绝、retune、回滚、停止、降级、连续失败上限、最小收益阈值、剩余时间判断和预算耗尽策略。


B6：完成领域层测试：测试参数边界、非法参数、模型卡版本差异、QA 严重失败、拟合退化、回滚、停止条件、双器件评分、参数快照恢复和策略可复现性；领域层不得依赖 MCP、LLM、LangGraph、数据库或具体 SDK。


GATE-A：领域层验收：领域测试全部通过；每次参数修改都有边界检查；候选退化时能够回滚；模型卡历史版本不会被覆盖；QA、评分、停止、预算和恢复规则在相同输入下可复现。

C：端口、基础设施与治理

C1：定义 Modeling MCP 端口：抽象健康检查、认证、会话创建与关闭、数据清点、模型选择、模型初始化、参数读取、参数设置、冻结、解冻、拟合、拟合结果查询、QA、可视化和模型卡导出能力；不绑定官方工具名称；规定统一鉴权、超时、限流和结果转换边界。


C2：定义国产大模型端口：抽象结构化推理请求、事实输入、候选动作、模型信息、Token 统计、提示词摘要、耗时、超时、限流、非法输出、备用模型切换和模型路由结果。


C3：定义持久化与遥测端口：抽象运行状态、LangGraph 状态、检查点、模型卡版本、决策记录、JSONL 事件、指标、Trace、心跳和调用追踪；保证日志不是唯一状态来源。


C4：开发运行时与配置模块：实现环境变量读取、密钥注入、模型配置、MCP 地址配置、预算配置、提参策略配置、QA 配置、运行目录初始化、系统时钟检查、deadline 初始化和启动前检查；Token、API Key 和正式测试数据不得进入仓库。


C5：开发日志与可观测性模块：实现会话、MCP 调用、LLM 调用、Agent 决策、参数变化、拟合、QA、retune、回滚、检查点、异常、暂停、恢复、心跳和产物导出日志；每条 Trace 至少包含 step、时间、模型、Token、动作、工具、结果、延迟、成本、trace_id 和 checkpoint_id；按模型层、上下文层、工具层、状态层、评测层和基础设施层归类失败。


C6：开发持久化与恢复模块：保存当前节点、当前参数组、候选模型卡、最佳模型卡、QA、误差、预算、重试次数、会话状态、任务状态、事件流和最终产物；实现幂等写入、最近 checkpoint 恢复、事件重放、进程重启恢复和恢复一致性检查。


C7：开发容错与长任务模块：实现 MCP 超时、服务端错误、返回格式异常、LLM 429、LLM 超时、非法结构化输出、拟合不收敛、会话丢失和磁盘异常的分类处理；临时错误使用指数退避和随机抖动，永久错误不重试，未知错误有限重试后降级；实现软终止、硬终止、任务心跳、deadline 传播和 Worker 恢复。


C8：开发治理、安全、成本与权限模块：实现 24 小时预算、MCP 调用预算、LLM Token 预算、重试预算、每器件有效 MCP 调用次数检查、国产模型调用检查、日志完整性检查、敏感信息检查和赛后开源要求；实现 L0 只读、L1 普通副作用、L2 高风险操作权限；增加 Prompt 注入防护、路径穿越校验、参数白名单、目录限制、上下文压缩、成本账本、预算告警和模型分级路由。


GATE-B：基础设施验收：端口定义稳定；日志可以关联器件、运行、任务、会话、决策、模型卡和 QA；进程中断后可恢复；软终止和硬终止行为明确；密钥不进入日志和产物；预算、权限、成本和反作弊检查可以在运行中执行。

D：本地仿真、数据与 Mock 环境

D1：建立本地 ASM-HEMT 仿真环境：接入可使用的 ASM-HEMT 模型、Verilog-A 或 SPICE 仿真器；准备参数模板、初始模型卡、偏置扫描、温度扫描和频率扫描；确认可以生成 DC I-V、C-V、Pulse I-V 和 S 参数开发数据。


D2：开发合成器件数据工厂：生成不同阈值电压、迁移率、导通能力、接触寄生、外部寄生、温度、自热、陷阱、动态导通、kink、场板和噪声水平的器件；同时生成缺测、异常点、边界参数和困难器件；保存数据来源、生成配置、版本和数据分割信息。


D3：开发本地评分与隐藏网格代理：在开发数据上建立 (Vgs, Vds, T) 网格验证；比较模型预测与 SPICE 真值；实现曲线归一化误差、QA 通过率、单器件得分和双器件得分代理；严格区分开发集、回归集和模拟隐藏集，防止验证数据泄漏。


D4：开发 Mock Modeling MCP 与工具注册表：模拟数据引用、模型选择、会话生命周期、参数操作、拟合执行、拟合结果查询、QA、可视化引用和模型卡导出；统一登记工具名、输入 Schema、输出 Schema、前置条件、参数边界、副作用、权限等级、超时、重试策略和执行器；所有返回统一使用 {status, data, error, metrics} 信封；支持延迟、超时、5xx、格式错误、会话失效、重复请求、幂等执行、沙箱和文件快照回退。


D5：开发 Fake LLM 与本地任务执行器：模拟提参计划、异常诊断、QA 解释、retune、回滚、弱器件调度和运行总结；测试非法 JSON、未知动作、未登记参数、重复动作、超时、限流、备用模型切换、MAX_STEPS、动作指纹和循环检测。


D6：完成端口和 Mock 契约测试：验证 Mock MCP、Fake LLM、持久化、遥测、工具注册表、任务队列和未来真实适配器符合端口定义；覆盖正常调用、错误返回、重复调用、断线恢复、超时、结果结构异常、随机杀进程、断网、路径穿越、资源超限和危险操作拦截；建立基础 golden AgentEval 集。


GATE-C：本地替代环境验收：本地 ASM-HEMT 仿真可重复运行；Mock MCP 覆盖项目需要的能力类别；工具返回结构统一；Fake LLM 的非法输出可被拦截；本地隐藏网格代理可评分；幂等、沙箱和故障恢复测试通过；无官方 MCP 时项目仍可继续开发。

E：确定性单器件建模

E1：开发启动应用层：实现启动前环境检查、配置加载、预算初始化、日志初始化、任务创建、器件运行上下文创建、MCP 会话创建、Worker 领取任务和运行开始记录。


E2：开发数据审计用例：检查 DC I-V、C-V、Pulse I-V、S 参数的存在性；检查单位、端口、偏置方向、温度、频率、器件尺寸、扫描网格、覆盖范围、缺测区间和异常点；生成数据质量报告和可执行建模模块清单。


E3：开发模型初始化与固定提参计划：选择 ASM-HEMT 版本和初始模型卡；建立参数冻结状态、提参顺序、目标曲线、参数边界和预算；封装 fit_dc_chain、fit_cv_chain、fit_self_heating_chain、fit_trap_chain 等 Skills；在此阶段不允许 LLM 自由修改计划。


E4：开发基础 DC 提参：处理转移特性、输出特性、低场和基础导通行为；提取阈值、亚阈值、导通电流、迁移率、饱和速度和串联电阻；每轮保存拟合结果、参数变化、误差、QA、Trace、checkpoint 和幂等调用键。


E5：开发电荷与 C-V 提参：提取栅电荷、端口电容及其偏置依赖；检查电荷连续性、电容趋势和物理合理性；禁止 C-V 优化破坏已经通过的 DC 基线。


E6：开发 S 参数与外部寄生提参：处理频率数据和端口一致性；提取接触寄生、外部寄生和本征参数；避免使用本征参数错误补偿外部寄生；记录频率范围、拟合目标和工具调用证据。


E7：开发温度与自热提参：组织多温度数据，提取温度相关参数和自热参数；检查温度升高、功耗增加后的电流、阈值和热效应趋势；设置温度策略和自热参数组的停止条件。


E8：开发 Pulse I-V、陷阱与色散提参：对齐静态和脉冲数据；提取动态导通、陷阱恢复、记忆效应和色散特征；要求动态参数调整不能明显破坏静态 DC；记录脉冲条件和恢复阶段。


E9：开发高场、kink 与场板提参：在基础 DC、C-V、寄生、温度和 Pulse 模块稳定后处理特殊高场行为；限制参数范围、尝试次数和允许动作，防止特殊参数掩盖基础模型错误。


E10：开发分组联合优化：按已完成参数组逐步解冻；联合比较全部已完成曲线、QA、误差、参数边界和本地隐藏网格代理；保留每轮候选模型卡、参数快照和动作指纹。


E11：开发 retune、回滚与停止流程：根据误差分布、QA 失败项、参数边界、已有曲线退化和最小收益判断接受、重调、缩小参数组、回滚或停止；连续失败达到上限后进入安全降级；不得无限重复同一动作。


E12：开发单器件运行管理：串联数据审计、模型初始化、固定提参、QA、retune、全局优化、最终验证和模型卡导出；每个关键节点写 checkpoint；支持单步心跳、deadline 传播、软终止、硬终止、暂停、恢复和用户批准受控动作。


E13：验收确定性单器件闭环：使用相同输入、配置和随机种子重复运行；确认模型卡可加载、基础 QA 可通过、结果可复现、候选可回滚、错误可恢复，并始终保留有效基线卡；将失败样本、Trace 和错误归因加入 AgentEval 回归集。


GATE-D：确定性建模验收：Mock MCP 下单器件可以从数据审计运行到模型卡导出；至少保留基线卡、最佳卡和候选卡；参数越界、拟合失败、QA 退化、服务中断和 Worker 重启都能被处理；相同输入下结果满足预设稳定性。

F：Agent 与状态机

F1：定义 Agent 决策契约：规定 Agent 可见事实、数据摘要、QA、误差、历史动作、剩余预算和可用参数组；规定允许动作、动作参数、决策理由、停止原因和结构化输出格式；使用 Pydantic 或 JSON Schema 严格解析，禁止信任自由文本。


F2：实现 Agent Loop：实现“模型决策 → Schema 校验 → 动作白名单校验 → 确定性执行器执行 → 统一工具信封回灌 → 状态更新”的循环；增加失败重试次数、MAX_STEPS、单步超时、总步数、总成本、总 Token、deadline 和动作指纹检测。


F3：开发 LangGraph 状态与检查点：定义 messages、task_state、meta 三类状态；保存运行状态、器件状态、当前节点、当前参数组、候选模型卡、最佳模型卡、QA 结果、预算、错误、重试和恢复信息；节点结束后持久化状态。


F4：开发 Agent 角色与 Skills：实现 Planner、Data Analyst、Extraction Strategist、QA Reviewer、Recovery Supervisor 和 Reporter；为不同角色绑定工具组合、提示词、参数组、权限和校验规则；每个角色只能基于事实提出允许动作，不能直接写入任意参数或绕过领域规则。


F5：开发 LangGraph 图与路由：实现 preflight、认证、建会话、数据清点、数据审计、模型初始化、计划、拟合、QA、决策、retune、回滚、联合优化、最终验证和导出节点；实现条件边、循环边、重试边、降级边、暂停边、恢复边和安全终止边。


F6：开发国产大模型适配器并完成单器件 Agent 全流程：先使用 Fake LLM 验证状态机，再接入主国产模型和备用国产模型；实现结构化输出解析、动作白名单检查、参数组校验、权限校验、Token 统计、超时、限流、Prompt 注入测试和确定性降级；确认 Agent 可以自主选择计划、参数组、retune 和恢复动作，但不能绕过参数边界、QA、回滚或产物校验；完成单器件 AgentEval。


GATE-E：Agent 验收：Agent 图结构清晰可审阅；所有输出经过结构和领域双重校验；状态可持久化和恢复；主模型失败时能切备用或执行确定性降级；重复动作、预算超限、Prompt 注入和非法工具调用能够被拦截；单器件 Agent 可以完整运行。

G：双器件、交付与测试日运行

G1：开发双器件协调模块：为两个物理特征不同的测试器件分别维护运行状态、MCP 会话、任务队列、Worker、模型卡版本链、日志、检查点和错误状态；两个器件不得交叉使用参数卡、数据和事件流。


G2：开发 24 小时预算调度与任务 Worker：实现任务创建、队列领取、Worker 心跳、任务释放、断点恢复和 deadline；设计测试日的启动、数据审计、基线、参数提取、联合优化、弱器件优化、冻结和提交时间门禁；根据 MCP 并发限制决定串行、交替或受限并行。


G3：开发双器件弱项优先策略：保证两个器件先分别获得可提交基线，再轮流优化；根据 QA、RMSE、风险、剩余时间和剩余调用预算识别较弱器件；禁止第一套器件消耗全部预算后才开始第二套。


G4：开发正式交付物与竞赛运行入口：生成每个器件的模型卡、独立运行日志和全局运行总结；检查器件映射、文件命名、版本号、日志完整性、MCP 调用证据、国产模型调用证据和敏感信息；提供统一启动入口、配置模板、依赖说明、健康检查、正常运行、恢复运行、提前停止、产物校验和故障处理命令。


G5：完成双器件 Mock 全流程演练：模拟两器件独立运行、基线建立、轮流优化、弱器件优先、预算切换、服务异常、暂停、用户批准、恢复和最终导出；验证两份模型卡、两份日志和一份总结相互一致；确认一侧器件故障不会污染另一侧状态。


GATE-F：双器件与交付验收：两器件均能独立完成流程并生成可提交基线；预算调度能优先保障较弱器件；每器件都有完整日志和事件链；模型卡、日志和总结的版本与统计一致；提前停止、暂停恢复和故障隔离可以生成有效提交包。

H：官方 MCP、回归与最终发布

H1：登记官方 MCP 与交付契约：官方资料到位后录入工具清单、请求响应、错误码、会话寿命、认证 Token、限流、并发、断线恢复、数据访问方式、ASM-HEMT 版本、模型卡 Schema、QA Checklist、日志要求、提交规范和官方开源要求。


H2：开发 Primarius MCP 适配器：实现官方认证、健康检查、会话创建与关闭、服务端数据引用、工具参数映射、响应解析、统一返回信封、错误归一化、重试、幂等判断和调用证据记录；不把官方细节泄漏到领域层。


H3：完成官方 MCP 集成验证：先验证健康检查、鉴权、建会话、数据清点、模型选择、模型初始化、参数操作、拟合、QA 和模型卡导出，再验证确定性单器件、Agent 单器件和双器件流程；确认每个器件有效 MCP 调用不少于规则要求，并确认全程存在指定国产模型调用证据。


H4：完成最终回归、竞赛演练与版本冻结：扩充回归器件、困难数据、缺测数据、服务错误、LLM 限流、拟合不收敛、会话丢失、进程重启和硬终止测试；执行 AgentEval、Trace 失败归因、失败样本回归闭环、成本优化、弱器件优化、干净环境安装、24 小时演练、提前终止和故障恢复；最终冻结代码、依赖、配置、提示词、工具注册表、Agent 图、Schema 校验器、运行手册和提交包；提交两份模型卡、两份独立运行日志和一份运行总结；若获得奖项，按赛题要求在规定时间内以 MIT、Apache 2.0 等许可完成开源发布。


GATE-G：官方环境验收：官方 MCP 最小链路通过；真实服务可以从数据清点运行到模型卡导出；官方模型卡 Schema 校验通过；模型卡可被指定仿真链加载；每个器件的 MCP 调用和国产模型调用都有真实审计证据；官方错误、会话恢复和限流规则已完成验证。


GATE-H：最终发布验收：两器件均稳定生成模型卡；QA、隐藏网格代理、日志、预算、AgentEval、故障恢复和 24 小时演练全部通过；干净环境可启动；最终交付物完整；代码、依赖、配置、Agent 图、提示词、工具契约和运行手册完成冻结；冻结后禁止引入未经回归验证的新策略