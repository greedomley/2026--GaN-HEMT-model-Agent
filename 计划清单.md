# GaN-HEMT 紧凑模型建模 Agent 竞赛执行计划清单

> 适用项目：`gan-hemt-modeling-agent/gan-hemt-modeling-agent`  
> 依据：官方赛题指南、项目 `docs/01`～`08`、其余 4 份项目分析文档、随仓库附带的 ASM-HEMT 101.0.0 参考实现  
> 当前基线：项目仍是文档与测试规范骨架，已补充 Agent 性能/恢复/完成/失败控制设计，但尚无实现代码、依赖声明、配置实值、可执行入口、自动化测试或官方 MCP 契约  
> 使用原则：官方 Q&A、官方 Schema 和官方 MCP 契约始终拥有最高优先级；官方信息变化时，先更新契约与决策记录，再修改实现和本清单

## 0. 如何使用本清单

- `[ ]` 表示未完成，`[x]` 表示已完成；只有“实现、验证、证据”同时具备时才可勾选。
- 优先级分为：**P0 必须完成**、**P1 显著影响成绩**、**P2 有余力再做**。不得用 P2 工作挤占 P0/P1。
- 任务编号中的第一个部分（如 `P2-C03`）是本清单的执行阶段与工作包；粗体中的第二个 `P0/P1/P2` 是优先级。它们与 `docs/01-overall-development-plan.md` 的 P0～P9 不是同一套阶段编号，必须通过第 3 节对照，不得直接等同。
- 实现文件必须按第 2.2 节落入对应源码、配置、数据或根目录位置；验证证据主要落入 `docs/`、`contracts/`、`tests/`、`artifacts/` 或 `operations/`，不能只靠口头确认。
- Mock、Fake LLM 和本地代理评分通过，只代表开发验证通过，不能标记“官方环境已通过”。
- 任何未经官方确认的工具名、字段、参数范围、评分归一化方式和提交命名都必须标记为“暂定”。
- 默认不开发前端。只有官方提交要求或演示确有必要时，才增加只读运行看板；前端不得影响主流程交付。
- 两名成员建议分工：**建模负责人**负责 ASM-HEMT、数据、拟合、QA 与评分代理；**Agent 负责人**负责工程基线、MCP/LLM、LangGraph、持久化与交付。契约、双器件演练和正式提交必须双人复核。

## 1. 不可违反的赛题约束

- [ ] **RULE-01｜P0｜建立官方事实台账。** 明确目标模型为 ASM-HEMT，建模工具为 MeQLab，外部能力由 Primarius Modeling MCP Server 暴露，Agent 框架限定为 LangGraph/deepagents，LLM 限定为国产大模型 API。  
  验收：`docs/` 中分别维护“官方已确认、项目暂定、待 Q&A、已废弃”四类事项，并记录来源、版本与确认日期。
- [ ] **RULE-02｜P0｜固化输入与数据边界。** 正式输入包含 DC I-V、C-V、Pulse I-V、S 参数；正式测试原始数据始终在组委会服务器侧，不复制到本地。  
  验收：领域对象只保存服务端引用、允许的摘要和产物；日志、缓存、异常转储中均无正式原始数据。
- [ ] **RULE-03｜P0｜固化评分目标但不猜归一化规则。** 单器件代理为 `QA_pass_rate × (1 - Curve_RMSE_normalized)`，总分代理为 `0.7 × mean(Card_1, Card_2) + 0.3 × min(Card_1, Card_2)`。  
  验收：本地评分明确标记为 `proxy`；官方公式未确认前，不自行裁剪 RMSE、不假定曲线权重或 QA 等权。
- [ ] **RULE-04｜P0｜反作弊硬门禁。** 任一器件 MCP 工具调用少于 10 次或全程未调用指定国产大模型 API，均可能导致该组 0 分。  
  验收：按器件维护 MCP 合规账本，目标预留安全余量为“至少 12 次有建模意义且成功留证的真实调用”，但官方最低规则仍单独显示为 10；国产模型至少有一次成功、可审计且参与真实决策的调用。是否计入失败调用以官方 Q&A 为准。
- [ ] **RULE-05｜P0｜固化提交边界。** 测试日前提交完整代码仓库、Agent 图/状态定义、Agent 主入口、依赖声明和架构图；测试日结束前提交 2 份模型卡、2 份独立运行日志和 1 份运行总结。  
  验收：提交前自动校验数量、器件映射、Schema、版本、摘要、日志统计一致性和敏感信息。
- [ ] **RULE-06｜P0｜获奖后开源合规。** 获奖代码须在赛后 7 天内按官方要求公开。  
  验收：自研代码许可证、第三方许可证清单、NOTICE、SBOM 和发布范围通过人工复核。

## 2. 当前项目基线与范围

- [ ] **BASE-01｜P0｜确认仓库来源。** 当前工作目录未发现 `.git` 元数据；先确认是否为解压副本、是否存在上游仓库和历史，再决定初始化或重新克隆，避免丢失来源信息。
- [ ] **BASE-02｜P0｜登记真实完成度。** 将“只有文档骨架、没有代码/依赖/脚本/测试/官方契约”写入项目状态页，后续每个里程碑更新一次。
- [ ] **BASE-03｜P0｜隔离第三方 ASM-HEMT。** 随仓库的 `ASM-HEMT-main` 为 101.0.0 参考实现，源码采用 ECL-2.0；它不能被当作自研 MIT/Apache 代码重新授权。  
  验收：明确其用途是参考、开发仿真或测试夹具；保留原 LICENSE/NOTICE；官方使用版本未确认前，不把 101.0.0 写死为正式版本。手册、研究论文、视频链接等非源码资料逐项核对再发布权限，权限不明时不随获奖开源仓库发布，只保存来源与获取说明。
- [ ] **BASE-04｜P0｜控制项目范围。** 首要目标是可运行、可恢复、可审计的建模 Agent，不做无关前端、复杂分布式平台、通用 Agent 平台或大模型微调。
- [ ] **BASE-05｜P0｜确认唯一项目根目录。** 当前材料根目录中还嵌套了两层同名目录；实际工程根目录应明确为包含 `README.md`、`src/`、`docs/`、`contracts/` 等目录的最内层 `gan-hemt-modeling-agent/gan-hemt-modeling-agent/`。`pyproject.toml`、锁定文件、`.gitignore`、许可证和 CI 均应落在该工程根目录，不能误放到赛题 PDF 与分析文档所在的材料目录。
- [ ] **BASE-06｜P0｜确定计划清单唯一来源。** 当前《计划清单.md》位于工程根目录之外。建立正式仓库时，应选择“继续将本文件作为外部主清单”或“迁入工程 `docs/` 并作为唯一主版本”之一；禁止外部与仓库内维护两份可独立修改的清单。

### 2.1 当前目录审计结论

| 位置 | 当前实际状态 | 与计划的关系 |
|---|---|---|
| 材料根目录 | 官方赛题 PDF、5 份 Markdown 和项目目录 | 只保存赛题材料与总计划，不放运行代码、密钥或正式产物 |
| 工程根目录 | `README.md`、8 份 `docs`、8 个支撑目录 | 未来版本控制、依赖、源码、测试和交付文档的唯一根目录 |
| `src/` | 只有 `README.md` | 9 个逻辑层尚未落盘，不得视为已有实现 |
| `contracts/`、`config/`、`data/`、`artifacts/`、`tests/`、`operations/` | 主要为占位 README；已增加运行控制契约、策略、四类测试与处置手册的规范 | 这些仍是待实现规格，不得将目录或 README 视为功能已经完成 |
| `ASM-HEMT-main/ASM-HEMT-main/` | 含 101.0.0 手册、Verilog-A、论文和 ECL-2.0 许可证 | 第三方参考资产，不属于 `src/`，不继承自研代码许可证 |
| `.git`、`pyproject.toml`、锁定文件、CI | 不存在 | 必须从工程基线任务开始建设 |

### 2.2 目标目录落盘契约

以下是计划任务与现有目录的一一对应关系。创建文件前先遵守该表，避免把同一职责分散到多处。

| 现有目录 | 目标内容 | 必须避免 |
|---|---|---|
| 工程根目录 | `pyproject.toml`、锁定文件、README、许可证、`.gitignore`、CI 配置和统一命令入口 | 把这些文件放到外层材料目录；存在两个可独立启动的工程根目录 |
| `docs/` | 官方事实/假设台账、架构源图、决策记录、当前状态、阶段报告、竞赛运行说明 | 把未实现架构写成已完成；与外部计划维护两份真源 |
| `src/` | entrypoints、orchestration、agents、application、domain、ports、adapters、infrastructure、governance 九个逻辑层的实现 | 直接把第三方 ASM-HEMT 源码、正式数据或运行产物放进源码包 |
| `contracts/` | `mcp/`、`model_card/`、`run_log/`、`run_summary/`、`decision_record/` 的官方契约索引，以及 `runtime_control/` 的内部阶段/操作/检查点/完成/失败/性能 Schema；仅在允许再分发时保存官方原文 | 内部运行控制字段冒充官方 Schema；把受保密或禁止再分发的官方文件提交 Git |
| `config/` | 无密钥的环境模板，以及模型、提参、QA、预算和 `policies/` 下性能/恢复/完成/失败控制实例值；仅引用 `contracts/runtime_control/` 的 Schema 版本 | 在配置目录维护第二份 Schema 真源；Token、API Key、正式数据引用和个人绝对路径 |
| `data/` | `synthetic/`、`regression/`、`hidden_sim/`、`fixtures/`、`manifests/`；版本控制生成器、清单与必要黄金夹具，大规模可重建数据默认忽略 | 组委会正式原始数据；把模拟隐藏集真值暴露给 Agent；无清单的大体积二进制数据 |
| `artifacts/` | 按 `runs/<run_id>/devices/<device_id>/` 隔离 cards/logs/qa/checkpoints；全局使用 `summary/`、`quality/`、`package/`、`submission/`，避免把唯一全局总结放入器件目录；默认忽略实际运行目录 | 把大规模运行产物默认提交 Git；覆盖冻结产物；两器件或器件/全局产物混用目录 |
| `tests/` | `unit/`、`contract/`、`scientific/`、`integration/`、`e2e/`、`performance/`、`recovery/`、`completion/`、`failure_control/`、`regression/`、`rehearsal/` | 只测试“程序不报错”而不验证物理、耗时、恢复、完成判定和证据链 |
| `operations/` | preflight、run、resume、early-stop、freeze、validate、package，以及 Agent 减载、未知结果对账、失败处置和完成判定手册/脚本 | 把核心建模算法写进运维脚本或依赖人工隐式步骤 |
| `ASM-HEMT-main/ASM-HEMT-main/` | 保持第三方来源、版本、LICENSE/NOTICE 和用途记录；需要重排位置时单独做可追溯迁移 | 删除许可证、改写来源、作为自研包直接发布；未核对权限就公开论文/手册等非源码资料 |

Python 物理目录尚未最终决定。现有文档把逻辑层写成 `src/entrypoints` 等；工程实现更推荐使用单一命名空间 `src/gan_hemt_modeling_agent/<logical_layer>/`，避免 `domain`、`agents` 等通用顶层包名冲突。第一批代码落盘前必须二选一，并在同一次变更中同步 `README.md`、`docs/02-system-architecture.md`、`docs/03-module-catalog.md` 和 `src/README.md`；禁止两种布局混用。

## 3. 总体依赖顺序与阶段门禁

### 3.1 与项目 `docs/01` 阶段的对照

| 本清单执行阶段 | 主要内容 | 对应 `docs/01` 阶段 |
|---|---|---|
| 清单 P0 | 官方事实、赛务、工程与目录基线 | 文档 P0 + P1 |
| 清单 P1 | 本地仿真可行性、合成数据前置能力 | 文档 P3 的数据前置 + P6 的数据工厂前置 |
| 清单 P2 | 内部契约、Mock、日志、持久化与恢复 | 文档 P2 的替代环境 + P4 的基础设施 |
| 清单 P3 | 非 Agent 基线与确定性领域引擎 | 文档 P3 + P4 |
| 清单 P4 | LangGraph 与国产大模型 Agent | 文档 P5 |
| 清单 P5 | 官方 MCP 最小闭环与真实适配 | 文档 P2 的官方环境闭环 |
| 清单 P6 | 合成回归、评分优化、双器件与容错 | 文档 P6 + P7 |
| 清单 P7 | 冻结、演练与正式测试日 | 文档 P8 + P9 |

这种交叉安排是有意的：合成数据和 Mock 必须提前支持确定性基线，而官方 MCP 的真实闭环可能因开放时间较晚后移。进度汇报时必须同时写“清单阶段”和“docs/01 阶段”，例如“清单 P3 / 文档 P3+P4”，避免编号误解。

```text
P0 官方事实/工程基线
  ├─→ P1 本地仿真可行性与合成数据
  └─→ P2 内部契约、Mock、日志与恢复
          ↓
P3 确定性单器件闭环
          ↓
P4 LangGraph + 国产大模型 Agent
          ↓
P5 官方 MCP 契约映射与真实集成
          ↓
P6 双器件盲测、鲁棒性与故障演练
          ↓
P7 交付冻结与 24 小时正式运行
```

P0 的官方问题跟踪贯穿全程；官方 MCP 未开放不阻塞 P1～P4，但没有通过 P5 不得宣称项目具备参赛交付能力。

交叉依赖：`P2-B04` 只有在 `P1-A02` 已提供最小可运行仿真/拟合后端后才能联合验收；P1 与 P2 其余工程工作可并行，但不得用硬编码 Mock 成功替代该依赖。

## 4. P0：官方契约、赛务与工程基线

### 4.1 官方材料和赛务

- [ ] **P0-A01｜P0｜完成报名与团队资格检查。** 核对参赛成员、指导教师、报名平台、截止时间、测试日代码仓库截止点和最终上传方式；以官方平台/Q&A 为准，不只依赖整理文档。
- [ ] **P0-A02｜P0｜取得并归档赛题 Q&A。** 保存原文件、来源链接、下载日期和文件摘要；每次更新做差异记录。
- [ ] **P0-A03｜P0｜提交最高优先级问题。** 优先询问：官方模型卡 Schema、ASM-HEMT 版本、MCP 工具/错误/会话契约、数据可见形式、QA Checklist、RMSE 归一化、并发限流、有效调用统计、LLM 审计字段和提交规范。
- [ ] **P0-A04｜P0｜申请官方开发资源。** 获取 MeQLab/MCP 开发端点、认证 Token 申请流程、样例数据或接口样例；记录有效期、网络白名单和服务时间窗。
- [ ] **P0-A05｜P0｜建立变更决策记录。** 官方答复导致架构、字段或算法变化时，记录“原假设、官方结论、影响文件、迁移办法、复测结果”。

### 4.2 最小可维护工程

- [ ] **P0-B01｜P0｜建立版本控制与分支规则。** 确认上游后建立主分支、功能分支、标签和发布策略；禁止把密钥、正式数据、运行缓存和大体积临时产物提交进仓库。
- [ ] **P0-B02｜P0｜建立 Python 工程。** 固定 Python 3.10+ 的实际小版本，创建 `pyproject.toml`、锁定文件、源码包、测试配置和可安装入口；在 Windows 与目标 Linux 环境分别验证。
- [ ] **P0-B03｜P0｜建立最小质量命令。** 提供安装、格式检查、静态检查、类型检查、单元测试、契约测试、端到端测试、运行、恢复和产物校验命令；所有命令写入 README/operations。
- [ ] **P0-B04｜P0｜建立配置与密钥边界。** 提供无秘密的配置模板；密钥仅由环境或受控 Secret 注入；启动时只检查存在性，不打印值。
- [ ] **P0-B05｜P0｜建立 CI 基线。** 每次变更至少执行格式/静态检查、单元测试、Schema/契约测试和敏感信息扫描；仿真与官方 MCP 测试按环境标签隔离。
- [ ] **P0-B06｜P0｜形成代码一致的架构图。** 图中包含入口、LangGraph、领域引擎、MCP/LLM 端口与适配器、检查点、治理和双器件隔离；图可由版本化源文件重建。
- [ ] **P0-B07｜P1｜建立依赖与许可证台账。** 记录 Python、Agent 框架、LLM SDK、仿真器、ASM-HEMT 和其他第三方依赖的版本、来源、许可证与发布限制。
- [ ] **P0-B08｜P0｜落地目录骨架并同步文档。** 在唯一工程根目录中选定 Python 包布局，创建九个逻辑层、六类契约子目录、策略、数据/产物/性能与可靠性测试/运维子目录；同时配置空目录保留与 Git 忽略规则，并同步 README、架构、模块目录和本清单的路径表。  
  验收：自动目录检查脚本逐项确认“应存在、应忽略、禁止出现”的路径；源码、第三方资产、开发数据、运行产物和正式数据边界无交叉。

### GATE-P0

- [ ] 干净环境可安装空工程并运行最小测试。
- [ ] 官方事实、假设、阻塞问题和赛务截止点均有负责人及复核日期。
- [ ] 仓库无密钥和正式测试数据；第三方 ASM-HEMT 许可证边界明确。
- [ ] 唯一工程根目录、Python 包布局、计划清单真源和目录忽略规则均已确定，并与 README/docs 同步。
- [ ] 架构图与目录职责一致，不把尚未实现的模块表述为已完成。

## 5. P1：ASM-HEMT 本地仿真与合成数据可行性

### 5.1 仿真链验证

- [ ] **P1-A01｜P0｜建立参数知识基线。** 从随仓库手册和 `asmhemt.va` 提取版本、端口、参数名、默认值、单位、代码约束与模型开关，生成可追溯注册表；人工抽查 VOFF、NFACTOR、U0/UA/UB、VSAT、LAMBDA、ETA0、自热、陷阱和场板相关项。
- [ ] **P1-A02｜P0｜完成仿真器可行性 Spike。** 选择并固定一条合法的 Verilog-A/SPICE 工具链；先验证编译/加载和最小 DC 工作点，再分别验证 DC sweep、温度、AC/C-V、Pulse I-V 和两端口 S 参数能力。
- [ ] **P1-A03｜P0｜为不支持能力准备替代方案。** 若当前开源仿真链无法可靠生成某类数据，记录证据并切换已验证仿真器、离线夹具或官方样例；不得把未跑通的 Pulse/S 参数写成“已支持”。
- [ ] **P1-A04｜P1｜建立数值正确性检查。** 检查单位、偏置方向、端口、电流符号、温度、收敛容差和随机种子；用黄金网表或第二仿真链抽查关键曲线。

### 5.2 合成器件数据工厂

- [ ] **P1-B01｜P0｜定义器件参数空间。** 基于模型约束和物理关联生成参数组合，避免独立随机采样产生大量非物理解；每个器件保存 seed、参数真值、开关、仿真配置和生成版本。
- [ ] **P1-B02｜P0｜覆盖两类差异化器件族。** 至少覆盖阈值/导通能力差异，并逐步加入寄生、自热、陷阱、温度、高场、kink、场板、噪声、缺测和异常点；复杂效应必须有明确开关和标签。
- [ ] **P1-B03｜P0｜隔离数据集。** 建立开发集、回归集、模拟隐藏集；模拟隐藏集的参数真值不能进入 Agent 上下文、提示词或策略调参过程。
- [ ] **P1-B04｜P1｜建立数据清单与完整性摘要。** 记录每类曲线、网格、温度、频率、脉冲条件、文件摘要和生成环境；生成结果可按 seed 重建。
- [ ] **P1-B05｜P1｜建立本地评分代理。** 实现逐曲线误差、QA 代理、单器件/双器件代理分数及分项报告；所有未知官方权重均配置化并标记为暂定。

### GATE-P1

- [ ] 同一环境与 seed 可重复生成一致数据或在预先定义的数值容差内一致。
- [ ] 至少两类差异化器件可完成最小 DC 仿真；其他数据类型逐项标记“已验证/替代/阻塞”。
- [ ] 模拟隐藏集无泄漏；评分报告明确区分官方得分和本地代理。
- [ ] ASM-HEMT 101.0.0 仅是当前开发参考，官方版本差异有迁移检查项。

## 6. P2：内部契约、Mock、可观测性与恢复

### 6.1 契约与端口

- [ ] **P2-A01｜P0｜定义内部标识。** 统一 `run_id`、`device_id`、`mcp_session_id`、`event_id`、`trace_id`、`span_id`、`parent_event_id`、`operation_id`、`attempt_id`、`retry_of_operation_id`、`model_card_version`、`checkpoint_id`、`idempotency_key` 和 `request_fingerprint`；所有重试、关键路径和恢复对账均可关联。
- [ ] **P2-A02｜P0｜定义供应商无关 MCP 端口。** 覆盖能力发现、会话、服务端数据引用/摘要/图像、模型选择、参数操作、拟合、QA、可视化引用和导出，并预留幂等键、异步任务提交、状态/结果查询、取消及能力声明；不得提前写死官方工具名或假定所有调用同步完成。
- [ ] **P2-A03｜P0｜建立能力驱动的数据契约。** Agent 必须能根据 MCP 实际返回的“原始数值、统计摘要、图像或仅引用”调整策略，任何用例不得默认能下载 NumPy 数组。
- [ ] **P2-A04｜P0｜定义国产 LLM 端口。** 统一结构化请求/响应、模型版本、Token、耗时、错误和路由结果；自由文本只能作为解释，不能直接成为可执行动作。
- [ ] **P2-A05｜P0｜定义模型卡、决策、QA、日志、总结与运行控制内部 Schema。** 在 `contracts/runtime_control/` 定义 `StageResult`、`OperationRecord`、`CheckpointManifest`、`CompletionVerdict`、`FailureRecord` 和 `PerformanceReport`；全部内部 Schema 与官方 Schema 分离，并带 `schema_version`、来源、兼容性和迁移策略。

### 6.2 Mock、Fake LLM 与故障注入

- [ ] **P2-B01｜P0｜实现最小 Mock MCP。** 先覆盖单器件闭环真正需要的能力，再增加可控延迟/长时间挂起、超时、5xx、部分/非法响应、会话失效、重复请求、幂等返回以及“服务端已执行但响应丢失”；不要先建设通用工具平台。
- [ ] **P2-B02｜P0｜实现可编程 Fake LLM。** 覆盖合法决策、非法 JSON、未知动作、未登记参数、重复动作、超时、限流和备用路由。
- [ ] **P2-B03｜P1｜建立端口契约测试。** 同一套测试分别作用于 Mock 与未来真实适配器，确保错误归一化和领域对象不依赖供应商字段。
- [ ] **P2-B04｜P0｜让 Mock 接入真实本地数值后端。** 本项显式依赖 `P1-A02` 的最小仿真/拟合后端。协议故障测试可使用预置返回，但科学闭环必须调用本地数值后端，而不是硬编码“拟合成功”；对 SciPy/lmfit/Optuna 等候选做小规模基准后固定主方案、边界、seed、超时与停止条件。

### 6.3 日志、状态与治理

- [ ] **P2-C01｜P0｜从第一天写 JSONL 审计日志。** 至少记录 session、MCP、LLM、decision、fit、QA、retune、rollback、checkpoint、recovery、error、completion、freeze；每条事件可关联器件、运行、会话、span/操作/决策/模型卡版本。持久化 UTC 起止、`process_epoch_id` 和同进程单调时钟计算的 duration，禁止跨进程比较单调时钟值；同时记录等待/执行耗时、canonical FailureRecord 和恢复动作。
- [ ] **P2-C02｜P0｜日志最小化与脱敏。** 保存参数摘要、结果摘要和必要的 prompt hash/受控摘要；不保存 Token、API Key、认证头、正式原始数据或无必要的完整提示词。
- [ ] **P2-C03｜P0｜建立独立状态存储与原子检查点。** 日志不是唯一状态来源；保存 LangGraph 状态、基线/最佳/候选/冻结卡、预算、错误和冻结状态。检查点使用临时写入、摘要校验和原子提交，记录状态/图/代码/配置/契约版本、操作账本水位和最近完整点；最新点损坏时可回退上一完整点。
- [ ] **P2-C04｜P0｜实现操作账本与幂等恢复。** 所有副作用执行 `intent → execute → confirm → checkpoint`。幂等键绑定不可变请求指纹、会话、工具/契约版本和基线卡；`attempt_id` 单列，同键载荷变化必须拒绝。`OUTCOME_UNKNOWN` 查询/重放复用原键；已确认无副作用的可重试失败按官方契约复用键或创建带 `retry_of_operation_id` 的新操作。恢复所有权按 `run_id + scope_id`，覆盖 `device:<id>` 与全局总结/打包/上传的 `global`；重复 resume 幂等。
- [ ] **P2-C05｜P0｜实现层级 deadline、运行预算与合规账本。** 分运行、器件、阶段和调用记录 UTC 绝对 deadline、已用/剩余时间、LLM/MCP/拟合/QA/检查点/重试预算及安全余量。3/6/17/20/22/23 小时是内部 SLO，超时触发减载但不自动使包无效；官方 24 小时/最终上传截止与 `submission_status` 是交付硬门禁。本地统计与服务端审计口径分开显示。
- [ ] **P2-C06｜P1｜实施针对性安全检查。** 参数/动作白名单、产物目录限制、路径规范化、密钥扫描和异常转储脱敏为必做；复杂权限分级、分布式 Worker 和通用沙箱仅在真实威胁或运行环境需要时实施。
- [ ] **P2-C07｜P0｜统一阶段、完成与失败口径。** 分离 `phase/execution_status/stage_result/device_verdict/pair_verdict/submission_status`；每阶段定义 `applicability` 与 `SUBMISSION_HARD/STRICT_QUALITY_REQUIRED/OPTIONAL`，QA 再区分官方硬门禁、项目安全硬门禁和软评分目标，未确认值标记暂定。单器件为四级结果，双器件包为 `STRICT_COMPLETE/VALID_DEGRADED_PACKAGE/INVALID_PACKAGE`，平台交付状态独立。完成由确定性校验器判定。canonical `FailureRecord` 统一 `failure_id/error_code`、stage/category、`retry_disposition`、`side_effect_state`、允许动作/选中 fallback、主因状态、一项主因、零到多项促成因素及关联 ID。
- [ ] **P2-C08｜P0｜建立性能事件与关键路径计算底座。** 定义统一字段 `time_to_first_valid_card_by_device`、`time_to_dual_baseline`、`time_to_best_valid_card_by_device`、`time_to_model_cards_frozen`、`time_to_valid_package`、`time_to_submission_confirmed` 和 `official_end_to_end_time`；实现 UTC/span、依赖图、等待/执行拆分、检查点开销和关键路径聚合，并用可控延迟的合成事件/最小夹具验证。P2 不冻结真实建模或双器件性能基线；真实顺序基线在 P6 建立，Mock 性能不得冒充官方 SLO。

### GATE-P2

- [ ] Mock/Fake 的正常、非法输出、超时、重复、会话丢失和进程重启测试通过。
- [ ] 状态、日志、操作账本、模型卡版本、完成判定、性能事件和合规账本可以通过 ID 完整对账；基于 P2 契约夹具可生成阶段漏斗、失败分布和关键路径，不宣称完整建模流程已实测。
- [ ] 恢复后不重复已提交副作用、不丢失最佳已验证模型卡；未知结果先对账，最新检查点损坏可回退，双恢复启动只有一个获得恢复权。
- [ ] 每个执行阶段均产生结构化结果，所有终态失败均落入 canonical FailureRecord；执行 `FAILED_SAFE`、器件结果、包结果和提交状态不会相互冒充。
- [ ] 领域层与应用层没有官方 MCP/具体 LLM SDK 字段泄漏。

## 7. P3：确定性单器件建模闭环

### 7.1 领域规则和模型卡版本

- [ ] **P3-A01｜P0｜实现领域对象。** 覆盖器件、服务端数据能力、运行、参数组、拟合候选、QA、评分代理、决策、预算、失败、单/双器件结果和终止原因；领域层不得依赖 LangGraph、MCP SDK、LLM SDK 或数据库。
- [ ] **P3-A02｜P0｜实现参数组注册表。** 每组定义所需数据、参数、单位、边界、依赖、冻结/解冻、目标、最大轮数、最小收益和不可辨识处理；参数名以当前参考实现为开发依据，以官方版本为最终依据。
- [ ] **P3-A03｜P0｜实现四级模型卡。** 至少保留初始卡、首份可提交基线、最佳已验证卡、当前候选/最终冻结卡；候选不得覆盖已接受版本。
- [ ] **P3-A04｜P0｜实现确定性接受门。** LLM 可以建议，但只有确定性规则可接受模型卡；比较目标误差、既有曲线退化、QA、参数边界、泛化代理和最小收益。

### 7.2 数据审计和提参顺序

- [ ] **P3-B01｜P0｜实现服务端数据审计。** 检查可用数据类型、单位、端口、方向、尺寸、偏置/温度/频率/脉冲覆盖、缺测与异常；输出“可执行参数组”和“不可辨识参数组”。
- [ ] **P3-B02｜P0｜建立默认科学顺序。** 默认从几何/模型开关和低场 DC 开始，再处理阈值/亚阈值、迁移率/接入电阻、高漏压 DIBL/饱和/CLM 与输出特性；随后根据数据能力进入 C-V、电荷、S 参数/寄生、温度/自热、Pulse/陷阱和高场/场板分支。
- [ ] **P3-B03｜P0｜允许有证据的重排。** 基础 DC 是硬前置；其他分支根据数据可用性、参数可辨识性、官方 MeQLab 能力和当前 QA 调整，不机械套用固定顺序。
- [ ] **P3-B04｜P1｜特殊效应按需启用。** 没有数据证据时不得启用陷阱、场板或复杂高场参数去补偿基础误差。

### 7.3 拟合、QA、retune 与停止

- [ ] **P3-C01｜P0｜完成最小 DC 闭环。** 从数据审计、模型初始化、单组拟合、QA、接受/回滚一直运行到模型卡导出；相同输入与配置可复现。
- [ ] **P3-C02｜P0｜逐步加入 C-V、S 参数、温度、自热、Pulse/陷阱和高场分支。** 每加入一类数据，先建黄金用例和退化测试，再接入全流程。
- [ ] **P3-C03｜P0｜实现回归式接受检查。** 每轮不仅检查当前曲线，也检查已完成曲线、关键 QA、参数接近边界程度和模拟隐藏集代理。
- [ ] **P3-C04｜P0｜实现有限 retune。** 每组有最大轮次、连续失败上限、最小收益、动作指纹和剩余时间门槛；指纹至少包含节点、工具、规范化参数、数据引用/证据版本和输入模型卡版本。同一证据下原样重复达到阈值即熔断，循环或退化时换已登记策略、缩小参数组、回滚或跳过可选分支。
- [ ] **P3-C05｜P0｜实现最终验证。** 检查内部/官方 Schema、仿真可加载性、QA、关键曲线、版本关联、日志连续性和产物摘要。
- [ ] **P3-C06｜P1｜建立单器件科学回归报告。** 固定记录 `device_verdict`、合法产物率、阶段到达/终态失败率、首轮通过率、QA、各曲线误差、单器件耗时原始值/max、拟合/回滚次数、失败类别和恢复结果；同版本样本 `n≥20` 时才按固定算法报告 P50/P95。成对包率与交付率留到 P6，不在 P3 提前验收。

### GATE-P3

- [ ] Mock MCP 下单器件可从数据审计运行到模型卡导出，且始终保留有效基线。
- [ ] 参数越界、拟合不收敛、QA 严重退化和不可辨识参数组都有确定性处理。
- [ ] 相同 seed 的关键参数、QA 和误差在定义的容差内可复现；容差写入测试而非口头约定。
- [ ] 新增复杂效应不会悄悄破坏已通过的基础 DC/C-V/寄生回归。

## 8. P4：LangGraph 与国产大模型 Agent

- [ ] **P4-A01｜P0｜定义 Agent 决策契约。** 输入仅含可验证事实、允许动作、历史摘要和剩余预算；输出含动作、受限参数、理由、置信/风险和停止建议，并经 Pydantic/JSON Schema 校验。
- [ ] **P4-A02｜P0｜实现 LangGraph 状态机。** 节点至少覆盖 preflight、认证、会话、数据清点/审计、初始化、计划、拟合、QA、决定、retune/回滚、全局优化、最终验证、模型卡冻结、包组装/验证、完成判定、包冻结、未知结果对账和安全失败；业务 phase、execution status、stage result、器件/包 verdict 与 submission status 分离。
- [ ] **P4-A03｜P0｜实现双重校验执行环。** `LLM 决策 → Schema 校验 → 动作/参数白名单 → 领域规则校验 → 操作意图落账 → MCP 执行 → 结果/错误归一化 → 结果确认或对账 → 状态/日志/原子检查点`；任何环节失败不得越过治理门，结构化错误须带允许修复动作并进入确定性路由。
- [ ] **P4-A04｜P0｜实现逻辑角色。** Planner、Data Analyst、Extraction Strategist、QA Reviewer、Recovery Supervisor、Reporter 可以是同一模型的不同受限节点，不为“多智能体”形式额外增加不必要进程。
- [ ] **P4-A05｜P0｜接入至少一种允许的国产模型。** 先完成真实 API 的最小成功调用与审计，再接全流程；主备模型、具体版本、结构化输出和缓存能力须符合官方范围。
- [ ] **P4-A06｜P0｜实现确定性降级。** LLM 限流/超时/非法输出时切换已验证备用模型或固定策略；降级不能删除已经产生的国产模型合规证据。
- [ ] **P4-A07｜P1｜建立 Agent 评测集。** 覆盖不同器件特征、数据缺失、参数越界、局部误差下降但 QA 退化、重复动作、预算不足、MCP 故障和恢复决策；同时断言最终结果等级、阶段漏斗、稳定错误码、动作轨迹和是否越过 deadline。
- [ ] **P4-A08｜P1｜导出可审阅决策图。** 架构图、LangGraph 实际节点/边、状态定义和日志节点名一致，并在 CI 中检查明显漂移。
- [ ] **P4-A09｜P0｜实现有界自愈与低轮次执行。** 确定性代码能完成的比较、校验、停止与恢复不调用 LLM；Schema/参数/工具选择错误只把脱敏可执行错误反馈给 Agent，默认最多修正 2 次。相同动作无新证据不得原样重试，必须换策略、确定性降级或安全停止；限制阶段 turn、输出 Token 和上下文事实量。只有官方允许、调用独立且可安全重放时才受限并行，禁止投机拟合和跨模型卡语义缓存。

### GATE-P4

- [ ] Agent 可自主完成单器件流程，所有执行动作均通过结构与领域双重校验。
- [ ] 主模型失败时可恢复/降级，非法工具、非法参数、重复循环和预算超限均被拦截；非法执行动作实际落地数和同证据原样重复超限数均为 0。
- [ ] 自愈不超过配置上限且每次改变动作/参数/策略；恢复、完成判定和物理 QA 不依赖 LLM 自评。
- [ ] Agent 与确定性基线在同一单器件 fixture/seed 上做配对 A/B；引入 LLM 后，单器件合法产物率、`DEVICE_COMPLETE` 率、QA 和恢复指标不越过预注册非劣阈值，耗时达到配置的改善/容差门槛。`n<20` 报全部样本与 max，`n≥20` 才报告 P95；不以“可解释”接受退化。
- [ ] 至少一次真实、允许的国产模型调用已成功并形成不含秘密的审计证据。

## 9. P5：官方 MCP 契约映射与真实集成

- [ ] **P5-A01｜P0｜归档官方契约。** 在 `contracts/` 保存工具、请求/响应、错误、会话、限流、模型卡、日志和总结 Schema 的原始版本、来源、日期及摘要。
- [ ] **P5-A02｜P0｜做差异分析。** 将官方契约与内部端口逐项映射，列出缺失能力、语义差异、不可用假设和迁移任务；禁止直接把官方字段传播到领域层。
- [ ] **P5-A03｜P0｜实现 Primarius 适配器。** 覆盖认证、能力发现、会话、数据引用、模型初始化、参数操作、拟合、QA、可视化和导出；按官方能力映射异步任务、幂等键、状态/结果查询、取消和会话续接，统一错误分类、分操作 deadline、限流和审计，禁止统一套用短超时。
- [ ] **P5-A04｜P0｜按风险递增验证。** 依次验证健康/认证、会话、只读数据清点、模型读取、受控参数操作、短拟合、QA、导出和恢复；逐操作实测耗时分布、并发/限流、超时后服务端状态、幂等/查询/取消/续接语义，并覆盖“远端执行成功但响应丢失”，每步保留脱敏证据。
- [ ] **P5-A05｜P0｜验证数据边界。** 根据真实返回形式调整选定包根目录下 `domain/dataset` 逻辑模块中的数据能力视图与 Agent 策略；确认缓存、日志、截图和异常转储不会把正式数据落地。正式物理路径以 P0-B08 选定的包布局为准。
- [ ] **P5-A06｜P0｜验证模型版本。** 对比官方 ASM-HEMT 版本与本地 101.0.0 的参数、默认值、开关和导出格式；必要时重建参数注册表、合成数据与回归基线。
- [ ] **P5-A07｜P0｜核对调用审计口径。** 区分调用尝试、成功调用、有效建模调用和服务端审计计数；正式运行目标保留安全余量，不用空调用凑数。
- [ ] **P5-A08｜P0｜在允许的开发数据上跑真实端到端。** GATE-P5 至少要求确定性单器件与 Agent 单器件通过；双器件真实集成留到 P6。没有授权开发数据时登记为外部阻塞并保留 Mock/契约证据，不得降为可选工作，也不得使用正式隐藏数据调参。

### GATE-P5

- [ ] 真实 MCP 可从数据清点走到模型卡导出，官方 Schema 与指定仿真链校验通过。
- [ ] 有授权开发数据时，真实确定性单器件和 Agent 单器件端到端均通过；若官方未提供开发数据，阻塞事实、已完成替代证据和测试日首次验证预案已双人确认。
- [ ] 官方错误、会话寿命、并发/限流、幂等、异步状态/查询/取消、超时后结果和恢复规则均有实测或明确的官方结论；Mock 数值未直接沿用为正式配置。
- [ ] 本地 101.0.0 与官方模型的版本差异已闭环，不存在静默参数映射。
- [ ] 服务端数据未违规落地；真实 MCP 和国产 LLM 证据可审计且不泄露密钥。

## 10. P6：双器件鲁棒性、回归与故障演练

- [ ] **P6-A01｜P0｜严格隔离两器件。** 独立保存会话、状态、检查点、模型卡版本链、日志、预算和产物目录；任何器件卡/数据引用不得交叉。
- [ ] **P6-A02｜P0｜先建立双基线。** 两器件完成数据审计后交替推进，确保都取得首份可提交模型卡，再进入精修。
- [ ] **P6-A03｜P0｜实现弱器件优先。** 使用 QA、分项误差、风险、剩余时间和可识别参数组决定预算；后半程优先改善较弱器件，不只优化平均值。
- [ ] **P6-A04｜P0｜选择最简单可行调度。** 根据官方并发限制选择串行、交替或受限并行；只有两器件或基于同一卡版本的独立只读操作可评估并行，同一器件的模型卡修改、拟合、接受和冻结保持单写者串行提交。只有真实关键路径证明需要时才引入任务队列/多 Worker。
- [ ] **P6-A05｜P0｜建立分环节故障矩阵。** 分别覆盖 preflight/auth、session、data audit、model init、planning/LLM、MCP/tool、fitting、QA/acceptance、loop/budget、persistence/recovery 和 artifacts/submission；场景至少含 MCP 超时/5xx/格式错误/会话丢失、远端已执行但响应丢失、LLM 429/超时/非法输出、拟合不收敛、磁盘不足、进程终止、网络中断、恢复后重复调用和时间不足。每例断言稳定错误码、重试资格、副作用状态和 fallback。
- [ ] **P6-A06｜P0｜完成成对盲测。** Agent 启动前冻结至少 10 组双器件 manifest、fixture/seed、版本和纳入分母；启动后的鉴权、MCP/LLM、崩溃与非法产物失败不得排除。只有启动前确认的夹具/测试基础设施无效可留证排除并补跑。分开记录严格包率、有效包率、模拟截止前交付率、合法卡率、阶段到达/终态失败率、QA、误差、最差卡、每次耗时/max、六类失败率及恢复三类指标；性能专项同版本 `n≥20` 时才算 P50/P95。
- [ ] **P6-A07｜P1｜做随机性稳定性测试。** 对代表器件使用多个 seed/模型采样配置重复运行，比较最终卡、轨迹、QA 和最差分；正式配置优先稳定而非单次最高分。
- [ ] **P6-A08｜P0｜执行完整恢复演练。** 在副作用意图后、远端成功本地落账前、候选接受/回滚、QA 后和冻结前后分别杀进程/断网；覆盖最新检查点损坏、状态 Schema 迁移、会话过期、恢复进程再次崩溃、两个恢复者同时启动和结果未知。除 `device:<id>` 外，覆盖全局总结、包冻结、上传和回执对账的 `global` 单写者；分别验证恢复安全不变量、deadline 内等价续跑和无法对账时安全遏制，`FAILED_SAFE` 不计续跑成功。
- [ ] **P6-A09｜P0｜用盲测驱动性能与失败闭环。** 在固定 manifest、数据、seed、代码/契约/配置版本上建立真实顺序基线；每轮只验证一个变量。配对 A/B 前冻结最小改善量、重复性容差和质量非劣阈值，只有关键路径达标且严格包率、有效交付率、QA、最差器件、恢复与合规均不过界才接受。分别跟踪 stage/operation/attempt 失败率、retry amplification、outcome unknown 和 self-heal success；每个意外失败生成脱敏夹具，以一项稳定主因和零到多项促成因素闭环 Top 1～3 后进入固定回归集。

### GATE-P6

- [ ] 最近 10 组成对盲测的有效包率为 10/10、严格包率至少 9/10，二者分别报告；同时给出 `n`、器件/困难场景覆盖和预选置信区间，明确这是最低工程门禁而非总体 90%/100% 可靠率证明。每个降级或失败样本都有一项稳定主因、零到多项促成因素、保底卡和回归用例。
- [ ] 两器件的状态、日志、模型卡与统计零交叉；一侧故障不破坏另一侧。
- [ ] 所有必测/随机注入的恢复安全不变量通过率为 100%；必测可恢复点等价续跑率为 100%，预登记且不少于 20 次随机注入时等价续跑率至少 95%；安全遏制单独统计。重复副作用、最佳卡丢失、作用域串扰、未分类终态失败和无界循环均为 0。
- [ ] 提前停止和时间不足路径能生成通过 `SUBMISSION_HARD` 的有效包；若只能 `DEVICE_FAILED_SAFE`，必须明确记为 `INVALID_PACKAGE`，不能计成功。
- [ ] P6 的 10 组盲测全部满足预先配置的实验室/加速时间预算并产出可重算关键路径；6/22/23/24 小时近正式绝对门禁只在 P7 演练验收，P6 不用少量样本伪造 P95 或宣称已证明 24 小时能力。

## 11. P7：交付冻结、24 小时演练与正式运行

### 11.1 测试日前冻结

- [ ] **P7-A01｜P0｜完成提交前仓库包。** 包含实际可运行代码、Agent 图/状态定义、主入口、锁定依赖、架构图、配置模板、安装/运行/恢复说明和许可证材料。
- [ ] **P7-A02｜P0｜完成干净环境安装。** 在从未配置过项目的目标系统按文档安装、启动、运行最小流程和恢复；记录命令、耗时和结果。
- [ ] **P7-A03｜P0｜完成两轮演练。** 至少一次加速全流程演练和一次接近正式约束的 24 小时演练；逐次而非用 P95 验收 6/22/23/24 小时绝对门禁。覆盖服务异常、LLM 切换、远端已执行但响应丢失、检查点损坏、器件/全局恢复争抢与再次崩溃、提前停止、包判定、冻结、上传和回执对账，并生成关键路径、失败与恢复报告。
- [ ] **P7-A04｜P0｜冻结发布候选。** 固定代码提交、依赖、模型、提示词、策略配置、工具映射、Schema、Agent 图和运行手册；冻结后只允许预先演练过的配置调整或故障恢复。
- [ ] **P7-A05｜P0｜完成提交渠道演练。** 验证文件大小、命名、压缩、上传、校验和、时区、官方截止判定和提交状态查询语义；仅准备官方允许且已演练的备用网络/上传入口及人工联系人，不临场发明绕过平台的路径。

### 11.2 正式 24 小时时间门禁

| 时间窗 | 硬目标 | 禁止事项 |
|---|---|---|
| 0–1 小时 | preflight、认证、MCP/国产 LLM endpoint 与结构化输出健康检查、两器件独立会话；健康调用是否计合规待 Q&A | 未验证环境直接长拟合 |
| 1–3 小时 | 两器件数据清点/审计、能力报告和初始计划；国产 LLM 参与至少一次有意义的整组计划/风险决策 | 把服务端原始数据落地；用无意义调用凑合规证据 |
| 3–6 小时 | 两器件各有首份可提交基线卡和检查点 | 第一器件无限精修 |
| 6–13 小时 | 依数据能力完成主要参数组 | 无边界全参数拟合 |
| 13–17 小时 | 全局 QA、有限 retune、形成各自最佳已验证卡 | 无限循环与无收益搜索 |
| 17–20 小时 | 双器件比较，优先改善较弱器件 | 给强器件独占预算 |
| 20–22 小时 | 完整验证、Schema/仿真/合规检查、冻结最终卡 | 引入未经回归的新策略 |
| 22–23 小时 | 由冻结卡组装/验证 2 卡、2 日志、1 总结，形成 `pair_verdict` 并冻结包 | 覆盖冻结卡或把内部字段强塞进未经确认的官方 Schema |
| 23–24 小时 | 上传、查询回执、人工双人复核和故障缓冲，截止前 `submission_status=CONFIRMED` | 非必要算法修改；把 `OUTCOME_UNKNOWN` 当已提交 |

### 11.3 最终产物校验

- [ ] **P7-B01｜P0｜模型卡校验。** 数量为 2、器件映射正确、官方 Schema 合法、指定仿真链可加载、版本与摘要固定。
- [ ] **P7-B02｜P0｜官方日志与内部遥测分别校验。** 两份正式日志只按官方 Schema/允许扩展生成并满足每器件 MCP 审计；内部 telemetry/quality report 对账 LLM、决策、参数、QA、操作账本、FailureRecord、恢复、完成、性能和冻结。内部字段只有官方允许时才映射进正式日志，冻结前未对账副作用为 0。
- [ ] **P7-B03｜P0｜官方总结与内部质量报告分别校验。** 一份正式总结只包含官方 Schema 允许的本次结果、最终卡引用和时间线；内部 `pair_verdict` 仅在官方允许时映射。盲测聚合严格包率/有效包率/交付率、关键路径、失败率与恢复统计留在内部 `quality/`，不得从单次正式运行伪造“率”。
- [ ] **P7-B04｜P0｜安全与完整性校验。** 无 Token/API Key、认证头、正式原始数据、缓存、调试转储或无关大文件；生成提交清单和 SHA-256 摘要。
- [ ] **P7-B05｜P0｜上传闭环。** 上传前写入 `UPLOADING`，上传后按官方查询语义下载/在线复核文件数量、摘要和可读性；只有平台明确确认且摘要一致才写入 `CONFIRMED`。回执超时保持 `OUTCOME_UNKNOWN` 并用原提交标识对账，禁止重复覆盖有效提交；保存平台确认信息与 UTC 时间。
- [ ] **P7-B06｜P0｜赛后开源准备。** 若获奖，在 7 天期限内发布自研代码；保留第三方 ECL-2.0 许可证和 NOTICE，不将其重新授权为 MIT/Apache 2.0。

### GATE-P7

- [ ] 测试日前仓库交付物完整，代码与架构图一致，干净环境可启动。
- [ ] 正式产物严格为 2 模型卡、2 独立日志、1 总结，全部自动校验和人工双人复核通过。
- [ ] 所有已列明故障点均满足恢复安全不变量，并等价续跑或确定性安全遏制；`FAILED_SAFE` 执行状态与器件/包结果分开，最后两小时不修改核心算法。
- [ ] 近正式演练逐次满足：≤6 小时双基线、≤22 小时模型卡冻结、≤23 小时有效包冻结、官方 24 小时截止前 `submission_status=CONFIRMED`；少量演练不计算 P95。
- [ ] 关键路径、严格/降级包、截止前交付、恢复三类指标、六类失败率、重复副作用和未分类失败门禁均由冻结版本的内部报告证明。
- [ ] 上传确认、文件摘要、合规证据和赛后发布边界均已留档。

## 12. 测试矩阵与最低证据

| 测试层 | 必须覆盖 | 最低证据 |
|---|---|---|
| 单元测试 | 参数边界、评分代理、接受/回滚、预算、版本链、脱敏 | 可重复测试报告 |
| Schema/契约测试 | 内部 Schema、canonical FailureRecord、Mock、官方适配器、错误映射、模型卡/日志/总结 | 版本化契约与测试结果 |
| 科学正确性测试 | 单位、符号、偏置、温度、参数可辨识性、曲线/QA 退化 | 黄金器件和容差说明 |
| 集成测试 | 仿真器、Mock MCP、国产 LLM、状态存储、产物导出 | 脱敏调用记录与产物摘要 |
| 端到端测试 | 确定性单器件、Agent 单器件、双器件 | 完整运行目录与报告 |
| 性能测试 | UTC/span、节点/LLM/MCP/拟合/QA/等待/检查点、关键路径、安全并行、3/6/17/20/22/23/24 小时门禁 | 固定 manifest、全部原始值/max；同版本 `n≥20` 才给 P50/P95；配对 A/B 非劣报告 |
| 故障/恢复测试 | 网络、MCP、LLM、拟合、进程、磁盘、deadline、结果未知、检查点损坏、器件/全局恢复争抢 | 安全不变量、等价续跑、安全遏制、操作账本和副作用对账 |
| 完成/失败控制测试 | applicability/requirement level、器件/包结果、submission status、canonical FailureRecord、有限自愈、熔断 | 预登记分母、判定明细、阶段漏斗、六类失败率和零未分类报告 |
| 回归/盲测 | 差异器件、困难器件、模拟隐藏集、多 seed、性能优化变量 | 严格/降级包与截止前交付趋势、最差值和失败 Top-N |
| 发布测试 | 干净安装、24 小时演练、提前停止、提交包、许可证 | 发布候选清单和复核签字 |

所有测试门槛必须在配置或测试代码中给出具体数值/容差。若官方未给精度阈值，先以当前稳定基线为比较对象，禁止用“效果不错”“基本通过”作为验收。

## 13. 建议里程碑与双人协作

时间统一以“正式测试集解锁日 D”为基准，并在官方平台确认实际日期后填入日历；不得只依赖非官方整理记录。

| 里程碑 | 建模负责人 | Agent 负责人 | 双人共同验收 |
|---|---|---|---|
| D-10～8 周 | 仿真链 Spike、参数注册表、合成数据最小集 | 工程基线、官方问题、内部契约 | GATE-P0/P1 最小项 |
| D-8～6 周 | DC 黄金器件、QA/评分代理 | Mock/Fake、日志、操作账本、完成/失败契约与性能埋点/聚合器 | GATE-P2 |
| D-6～4 周 | 分组提参、复杂效应回归 | 确定性编排、产物校验 | GATE-P3 |
| D-4～3 周 | 科学失败样本与策略 | LangGraph、国产 LLM、有界自愈与 Agent 评测 | GATE-P4 |
| D-3～2 周 | 官方模型差异与参数迁移 | 官方 MCP 适配与契约测试 | GATE-P5 |
| D-2～1 周 | 盲测、弱器件分析 | 双器件调度、关键路径、故障恢复与失败闭环 | GATE-P6 |
| D-1 周～D | 结果复核、模型卡验证 | 干净安装、冻结、提交演练 | GATE-P7 |

若官方 MCP 开放较晚，P1～P4 并行推进，但 P5 完成后必须留出至少一轮完整回归和一次提交演练。

## 14. 最高风险清单

| 风险 | 触发信号 | 预案 |
|---|---|---|
| 官方契约迟到或变化 | Schema/工具/QA 未公布 | 内部端口隔离；保留迁移层；不猜官方字段 |
| 本地仿真链能力不足 | Pulse/S/Verilog-A 无法验证 | 分能力 Spike；切换工具或使用已授权夹具 |
| ASM-HEMT 版本不一致 | 官方参数/默认值/导出差异 | 自动差异报告；重建注册表与回归基线 |
| 数据只返回摘要/图像 | Agent 无法访问采样点 | dataset 数据能力分支；使用服务端统计/拟合/QA |
| 参数耦合和过拟合 | RMSE 降但 QA/隐藏代理退化 | 分组解冻、全局回归、回滚和最小收益门槛 |
| MCP 延迟或会话丢失 | 长拟合超时、session 失效、远端结果未知 | 操作意图账本、幂等查询/对账、原子检查点、层级 deadline 和另一器件切换 |
| 国产模型不可用 | 429/超时/非法输出 | 已验证备用模型和确定性降级；保留合规证据 |
| 双器件失衡 | 一侧无基线或分数明显落后 | 6 小时双基线门禁；后半程弱器件优先 |
| Agent 轮次或环节失败过多 | 关键路径变长、重复动作、严格包率下降 | 确定性快路径、有界自愈、循环熔断、失败 Top-N 回归和不降质性能门禁 |
| 开源许可证冲突 | 发布时第三方代码混入 | 第三方隔离、SBOM、LICENSE/NOTICE 与发布审计 |
| 最后提交失败 | 网络/格式/文件错误或回执未知 | 23 小时前产物就绪；摘要校验、提交状态查询，以及仅使用官方允许且已演练的备用网络/上传入口 |

## 15. 项目最终完成定义

只有以下条件全部满足，项目才算“可高质量参赛”，而不是“Agent 可以运行”：

- [ ] 能在官方允许环境中对两套未知器件分别建立独立会话并完成数据审计。
- [ ] 能根据数据能力和物理依赖自主制定计划，但所有参数修改受确定性边界控制。
- [ ] 两套器件均能在 24 小时内生成可加载、Schema 合法、物理 QA 明确的完整 ASM-HEMT 模型卡。
- [ ] 严格包、降级有效包、无效包、执行失败安全和平台提交状态由确定性字段区分；`DEVICE_FAILED_SAFE` 或未确认提交不计竞赛成功。
- [ ] 官方截止前 `submission_status=CONFIRMED`，平台文件数量与本地冻结 manifest/SHA-256 一致；`OUTCOME_UNKNOWN` 不得视为已交付。
- [ ] 任意已列明的候选退化、MCP/LLM 故障或进程中断场景均可回滚/恢复并保留最近有效卡；副作用结果未知时先对账，重复副作用和最佳卡丢失均为 0。
- [ ] 双器件评分代理、最弱器件、关键路径、内部/官方 deadline、严格/降级包、截止前交付、六类失败率和合规调用均在运行中可见并受门禁控制。
- [ ] MCP、国产 LLM、决策、参数版本、拟合、QA、retune、回滚和冻结形成完整证据链。
- [ ] 测试日前仓库材料和测试日 2 卡/2 日志/1 总结均通过自动校验与双人复核。
- [ ] 无正式数据、密钥或第三方许可证违规，获奖后可以在 7 天内合规开源自研代码。

## 16. 从当前骨架立即开始的 12 项任务

以下顺序适合当前“只有文档骨架”的真实状态：

1. [ ] 确认唯一工程根目录、上游仓库与版本控制方式，决定 Python 包布局和计划清单唯一来源，建立安全的 `.gitignore` 与分支规则。
2. [ ] 从官方渠道取得/追踪 Q&A、MCP、模型卡、QA 和提交规范，并落实报名/指导教师/访问资源。
3. [ ] 建立 Python 工程、锁定依赖、最小 CLI、测试命令和 CI。
4. [ ] 完成第三方 ASM-HEMT ECL-2.0 许可证隔离与依赖台账。
5. [ ] 完成 ASM-HEMT 101.0.0 最小 DC 仿真 Spike，并逐项验证其他数据能力。
6. [ ] 从 Verilog-A/手册生成可追溯参数注册表和第一套黄金器件。
7. [ ] 定义内部运行、MCP、LLM、决策、模型卡、QA、日志、总结，以及阶段适用性/要求等级、操作、检查点、器件/包结果、提交状态、canonical FailureRecord 和性能 Schema。
8. [ ] 实现最小 Mock MCP、Fake LLM、JSONL 日志、关键路径计时、操作意图账本、原子检查点和未知结果恢复。
9. [ ] 先完成确定性 DC 单器件闭环，再逐步加入 C-V/S/温度/Pulse/特殊效应。
10. [ ] 在确定性闭环稳定后接入 LangGraph 和真实国产模型 API。
11. [ ] 官方 MCP 开放后完成契约差异、真实适配、版本迁移和端到端验证。
12. [ ] 完成预登记的 10 组成对盲测，分别验收严格/降级包率、模拟截止前交付、关键路径、恢复安全/续跑/遏制、六类失败率与 Top-N；再完成 24 小时绝对门禁、冻结、上传和回执对账演练。
